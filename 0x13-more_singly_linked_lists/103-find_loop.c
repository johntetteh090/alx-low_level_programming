#include "lists.h"
/**
 * find_list int_loop - function that finds the loop in a linked list
 * @head:
 * Return: return loop immediately
 */
listint_t *find_listint_loop(listint_t *head)
{
  head++;
  return (0);
}
