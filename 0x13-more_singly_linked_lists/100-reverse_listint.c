#include "lists.h"
/**
 * reverse_list int - define function
 * @head: describes the argument
 * Return: what does it return?
 */
listint_t *reverse_listint(listint_t **head)
{
	head++;
	return (0);
}
